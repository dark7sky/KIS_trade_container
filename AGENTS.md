# 에이전트 작업 시작 안내

이 저장소는 개인 한국투자증권 계좌에 실제 주문을 전송하는 Python MCP 서버다.
새 에이전트는 먼저 [인계 문서](docs/AGENT_HANDOFF.md), 다음으로 [운영 README](README.md)를 읽는다.
인계 문서에는 구조, 핵심 상태 알고리즘, 경로, 테스트, 배포 상태, 미해결 위험이 있다.

## 작업 원칙

- 시작할 때 `git status --short`, `git log -5 --oneline`으로 현재 상태를 확인한다. 기존 변경과 미추적 파일은 임의로 제거하거나 커밋하지 않는다.
- 기본 거래 모드는 **real**이며 모든 대화가 공유한다. 모드 변경도 계좌 운영에 영향을 준다.
- 사용자 요청 없이 실제 주문·취소·모드 변경을 테스트하지 않는다. 로컬 테스트는 가짜 브로커와 임시 DB를 사용한다.
- 주문 POST는 자동 재전송하지 않는다. `unknown`은 실패 확정이 아니다. 새 요청 ID로 재주문하지 않는다.
- `.env`, 계좌번호, 토큰, 원본 API 응답, 운영 DB를 문서·로그·Git에 넣지 않는다. Compose 설정 검증은 `config --quiet`를 사용한다.
- 영속 DB를 초기화하지 않는다. 백업·복구는 README를 따른다. 같은 DB로 서버를 여러 개 실행하지 않는다.
- 거래 상태나 재시도 로직 변경 시 재현 테스트를 먼저 실행하고, 부분체결·취소 경합·재시작·중복 요청을 함께 검토한다.
- 커밋 전 관련 테스트와 의존성 감사, 푸시 전 diff를 확인한다. 네트워크 쓰기·배포는 사용자 승인 범위 내에서만 한다.
- 사용자가 프로그램 변경 또는 업데이트를 요청해 커밋·배포까지 승인한 경우, 코드·`Dockerfile`·`compose.yaml`·운영 의존성 변경의 완료 조건은 다음 순서다: 관련 테스트와 감사 → `docs/AGENT_HANDOFF.md` 갱신 → 커밋 → `origin/main` 푸시 → Docker VM의 MCP 이미지 재빌드·재기동 → 컨테이너 health, 배포 코드 해시, 공개 `/healthz`, 미인증 `/mcp`의 `401` 확인. 어느 단계가 실패하거나 미실행이면 완료로 보고하지 않는다.
- 문서만 변경하거나 사용자가 읽기 검토만 요청한 경우에는 컨테이너를 재빌드하지 않는다. 실제 주문·취소·모드 변경은 배포 검증 항목이 아니며 별도 사용자 요청이 있어야 한다.
- 작업 종료 시 `docs/AGENT_HANDOFF.md`의 현황을 갱신한다. 코드 변경, 테스트 결과, 푸시, 실제 배포를 구분해서 기록하고 미검증 결과를 완료로 쓰지 않는다.

## 빠른 검증

```sh
.venv/bin/python -m pytest tests/test_trading.py -q
.venv/bin/python -m pytest -q
.venv/bin/python -m pip_audit
git diff --check
```

환경 준비, HTTP 통합 테스트 정지 이력, 커버리지 측정 방법은 인계 문서를 참고한다.
